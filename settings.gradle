/*
 * This file was generated by the Gradle 'init' task.
 *
 * The settings file is used to specify which projects to include in your build.
 *
 * Detailed information about configuring a multi-project build in Gradle can be found
 * in the user manual at https://docs.gradle.org/6.6/userguide/multi_project_builds.html
 */

pluginManagement {
    repositories {
        maven {
            url((System.getenv("ARTIFACTORY_URL") ?: "https://artifactory-poc.sp.workcover.vic.gov.au/artifactory").concat("/prd-imf-virtual/"))
            if (System.getenv("ARTIFACTORY_USER") || Project.hasProperty('artifactoryUser') ) {
                credentials {
                    username = System.getenv("ARTIFACTORY_USER") ?: artifactoryUser
                    password = System.getenv("ARTIFACTORY_PASSWORD") ?: artifactoryPassword
                }
            }
        }
    }
}

gradle.ext.projectGroup = "com.springml.spark.salesforce"
gradle.ext.projectName = "spark-salesforce"
gradle.ext.artifactoryUrl = System.getenv("ARTIFACTORY_URL") ?: "https://artifactory-poc.sp.workcover.vic.gov.au/artifactory"
rootProject.setName(gradle.ext.projectName)

